# TableManager
Permits the creation or modification of a table with addition of multiple columns at once, feature to add special columns (createdAt, CreatdBy, ModifiedAt, ModifiedBy, History, and their triggers )

